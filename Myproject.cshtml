<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Finance Tracker</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <link href="~/css/style.css" rel="stylesheet">
    <style>
        .header-image {
            background-image: url('../img.jpg');
            background-size: cover;
            background-repeat: no-repeat;
            background-position: center;
            height: 60vh;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 20px;
            position: relative;
        }

        .notification {
            position: fixed;
            bottom: 20px;
            left: 20px;
            background-color: rgba(0, 0, 0, 0.7);
            color: #fff;
            padding: 10px;
            border-radius: 5px;
            display: none;
            z-index: 1000;
            border: 1px solid white;
        }

        .team-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 70px;
        }

        .profile {
            text-align: center;
            margin: 10px 0;
            position: relative;
            width: 100%;
            max-width: 300px;
            border-bottom: 2px solid grey; 
            padding-bottom: 10px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

            .profile:hover {
                transform: scale(1.05);
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

        .profile-container {
            display: flex;
            justify-content: space-evenly;
            width: 100%;
            max-width: 620px;
            gap: 40px;
        }

            .profile-container:nth-child(2) {
                flex-direction: column;
            }

        .profile img {
            width: 100%;
            max-width: 150px;
            height: auto;
            border-radius: 50%;
        }

        .first-profile {
            align-self: flex-end;
        }

        .second-third-profiles {
            display: flex;
            justify-content: space-between;
            width: 100%;
            max-width: 620px;
            gap: 100px;
        }

        .last-profile {
            align-self: flex-start;
        }
        .team-section {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin-bottom: 70px;
            position: relative;
        }

        .profile {
            text-align: center;
            margin: 10px 0;
            position: relative;
            width: 100%;
            max-width: 300px;
            border-bottom: 2px solid grey; 
            padding-bottom: 10px;
            transition: transform 0.3s, box-shadow 0.3s;
        }

            .profile:hover {
                transform: scale(1.05);
                box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            }

        .profile-container {
            display: flex;
            justify-content: space-evenly;
            width: 100%;
            max-width: 620px;
            gap: 20px;
        }

            .profile-container:nth-child(2) {
                flex-direction: row;
            }

        .profile img {
            width: 100%;
            max-width: 150px;
            height: auto;
            border-radius: 50%;
        }

        .first-profile {
            align-self: flex-end;
        }

        .second-third-profiles {
            display: flex;
            justify-content: space-between;
            width: 100%;
            max-width: 620px;
            gap: 150px; 
        }

        .last-profile {
            align-self: flex-start;
        }

        .features-heading {
            position: absolute;
            left: 100px;
            top: 10px;
            transform: translateY(-50%);
            padding: 15px;
            background-color: black;
            font-style: italic;
            border-radius: 5px;
            transition: background 0.3s;
            cursor: pointer; 
        }

            .features-heading:hover {
                background: black;
            }

            .features-heading h3 {
                margin-bottom: 5px;
                cursor: pointer;
                transition: color 0.3s;
                font-size: 40px; 
            }

                .features-heading h3:hover {
                    color: lightgray;
                }

            .features-heading ul {
                list-style-type: none;
                padding: 0;
                margin: 0;
            }

                .features-heading ul li:hover {
                    color: lightgray;
                }

        .intro {
            text-align: center; 
        }
        .finance-heading h1:hover {
            color: #333; 
            cursor: pointer; 
        }
        .header-text:hover{
            color: lightgray;
            cursor: pointer;
        }
    </style>
</head>

<body>

    <div class="header-image">
        <div class="header-text">
            <p style="font-style: italic;">Finance Tracker</p>
        </div>
    </div>
    <div class="overlay"></div>

    <div class="container mt-0.5 content-wrapper">
        <div class="row">
            <div class="col-md-8 offset-md-2 intro text-center"> 
                 <h1>Welcome to Finance Tracker</h1>
                  <p>Organize your incomes, manage your budget, keep track of expenses and understand where all your money are with some great, yet simple features.</p>
            </div>
        </div>

        <div class="team-section">
            <div class="features-heading">
                <h3>Features</h3>
                
            </div>
            <div class="profile-container first-profile">
                <div class="profile">
                @*    <img src="cleans.PNG" alt="DR. STEPHEN LEWIS">*@
                    <h3>INTUITIVE & CLEAN</h3>
                    <p>
                        Intuitive, clean and simple way of adding daily expenses and incomes.

                    </p>
                   
                </div>
            </div>
            <div class="profile-container second-third-profiles">
                <div class="profile">
                   @* <img src="secure.PNG" alt="DR. ROBERT RUIZ">*@
                    <h3>SECURE</h3>
                    <p>Your data is always safe wherever your are and what ever device you are using.</p>
                   
                </div>
                <div class="profile">
                  @*  <img src="comp.PNG" alt="DR. MARY RODRIGUEZ">*@
                    <h3>COMPREHENSIVE</h3>
                    <p>A compact yet comprehensive list of expense and income categories. Add or edit options</p>
                    
                </div>
            </div>
            <div class="profile-container last-profile">
                <div class="profile">
                  @*  <img src="safee.PNG" alt="DR. JEAN CARTER">*@
                    <h3>RELIABLE & USEFUL</h3>
                    <p>Provides accurate financial data and tools for effective money management.</p>
                    
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-md-6 offset-md-3">
            <div class="calculate-form">
                <h4 class="text-center">Calculate Your Expenses</h4>
                <form id="expense-form">
                    <div class="form-group">
                        <label for="expense-name">Expense Name</label>
                        <input type="text" id="expense-name" class="form-control" placeholder="Expense Name" required>
                    </div>
                    <div class="form-group">
                        <label for="expense-amount">Amount</label>
                        <input type="number" id="expense-amount" class="form-control" placeholder="Amount" required>
                    </div>
                    <button type="submit" class="btn btn-primary">Add Expense</button>
                </form>
            </div>
            <div class="expense-table">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Expense Name</th>
                            <th>Amount</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody id="expense-list"></tbody>
                </table>
                <div class="total-amount">
                    <strong>Total:</strong>
                    $<span id="total-amount">0</span>
                </div>
            </div>
        </div>
    </div>

    <div class="notification" id="cookieNotification"></div>

    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

    <script>
        $(document).ready(function () {
            $('#expense-form').submit(function (e) {
                e.preventDefault();
                const expenseName = $('#expense-name').val();
                const expenseAmount = parseFloat($('#expense-amount').val());
                if (expenseName && !isNaN(expenseAmount)) {
                    $('#expense-list').append(`<tr><td>${expenseName}</td><td>$${expenseAmount.toFixed(2)}</td><td><button class="btn btn-danger btn-sm delete-expense">Delete</button></td></tr>`);
                    const totalExpenses = parseFloat($('#total-amount').text().replace('$', '')) + expenseAmount;
                    $('#total-amount').text('$' + totalExpenses.toFixed(2));
                    $('#expense-name').val('');
                    $('#expense-amount').val('');
                } else {
                    alert('Please enter valid expense name and amount.');
                }
            });

            $('#expense-list').on('click', '.delete-expense', function () {
                const row = $(this).closest('tr');
                const expenseAmount = parseFloat(row.find('td:nth-child(2)').text().replace('$', ''));
                const totalExpenses = parseFloat($('#total-amount').text().replace('$', '')) - expenseAmount;
                $('#total-amount').text('$' + totalExpenses.toFixed(2));
                row.remove();
            });
        });
    </script>
    <script>
        $(document).ready(function () {
            const cookieData = '@Model';
            if (cookieData.trim().length > 0) {
                const notification = $('<div class="notification"></div>');
                notification.text(cookieData);
                $('body').append(notification);
                notification.fadeIn('slow').delay(5000).fadeOut('slow');
            }
        });
    </script>

</body>

</html>
